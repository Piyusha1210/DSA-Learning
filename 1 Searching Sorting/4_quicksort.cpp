#include <bits/stdc++.h>
using namespace std;

void quickSort(int a[], int n)
{
    
}

void printArray(int a[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << a[i] << " ";
    }
    cout << endl;
}

int main()
{
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    // cout << "Quick Sort " << endl;
    // quickSort(a, n);
    // printArray(a,n);

    return 0;
}